class Search {
  constructor(timeFrom, timeTo, high, low, open, close) {
    this.timeFrom = timeFrom;
    this.timeTo = timeTo;
    this.high = high;
    this.low = low;
    this.open = open;
    this.close = close;
  }
}

export default Search;
